# Next.js Websocket Benchmark

A really simple app to benchmark the connection speed from client to server with results sent back in real time.

## Note: Set WS URL in .env

`NEXT_PUBLIC_WS_URL=ws://localhost:3000/api/ws` - replace localhost with your public url